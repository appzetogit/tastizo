# Tastizo Frontend

This frontend is powered by Vite and React.

Common scripts:

- `npm run dev` starts the local development server.
- `npm run build` creates the production build.
- `npm run lint` runs the frontend lint checks.
